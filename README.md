# Agenture

Webflow website project with Claude Code remote editing capabilities.

## Setup

1. Copy `.env.example` to `.env` and add your Webflow API key
2. Use Claude Code with the Webflow MCP server to edit the site remotely

## Environment Variables

- `WEBFLOW_API_KEY` - Your Webflow API key for the agenture project
